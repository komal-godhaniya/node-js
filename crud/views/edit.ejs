<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Book</title>
    <style>
        * {
            margin: 0px;
            padding: 0px;
            box-sizing: border-box;
        }
        body {
            font-family: 'Trebuchet MS', 'Lucida Sans Unicode', 'Lucida Grande', 'Lucida Sans', Arial, sans-serif;
            height: 100vh;
            width: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: rgb(250,243,153);
        }
        h1 {
            margin: 7px;
            margin-bottom: 20px;
        }
        form {
            height: auto;
            width: 40%;
            background-color: rgb(226,95,100);
            display: flex;
            flex-direction: column;
            border-radius: 20px;
            padding: 30px;
        }
        label {
            margin-left: 10px;
            margin-top: 10px;
        }
        input, select {
            background-color: white;
            padding-left: 9px;
            margin: 7px;
            width: 95%;
            border-radius: 10px;
            height: 40px;
            border: 1px solid transparent;
            color: grey;
        }
        button {
            height: 45px;
            width: 45%;
            border-radius: 10px;
            margin: 10px;
            margin-top: 20px;
            background-color: rgb(250,243,153);
            color: black;
            border: 1px solid transparent;
            font-size: 15px;
            cursor: pointer;
            font-weight: 600;
        }
        a {
            text-decoration: none;
        }
    </style>
</head>
<body>
    <form action="/updateData" method="post">
        <h1>Edit Your Book</h1>

        <input type="text" id="id" hidden value="<%= singleData.id %>" name="id">
        <input type="text" id="image" value="<%= singleData.image %>" name="image" placeholder="Enter image URL">
        <input type="text" id="name" value="<%= singleData.name %>" name="name" placeholder="Enter book name">

        <select name="category" id="category">
            <option value="<%= singleData.category %>"><%= singleData.category %> (Current)</option>
            <option value="classic">Classic</option>
            <option value="business">Business</option>
            <option value="fantacy">Fantasy</option>
            <option value="comics">Comics</option>
            <option value="fiction">Fiction</option>
            <option value="bio">Biography</option>
            <option value="self">Self-Help</option>
            <option value="romance">Romance</option>
        </select>

        <input type="text" id="author" value="<%= singleData.author %>" name="author" placeholder="Enter author name">
        <input type="text" id="year" value="<%= singleData.publishedyear %>" name="publishedyear" placeholder="Enter published year">
        <input type="text" id="price" value="<%= singleData.price %>" name="price" placeholder="Enter price">

        <div>
            <button id="addBookBtn" type="submit">Save Changes</button>
            <a href="/" style="display: inline-block; padding: 10px 15px; background-color: rgb(250,243,153); color: black; text-decoration: none; border-radius: 10px; text-align: center; font-weight: 600;">← Go back to Home</a>
        </div>
    </form>
</body>
</html>
